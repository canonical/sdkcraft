summary: SDKcraft plugs test to ensure SDKcraft can consume various valid plugs

restore: |
  run_sdkcraft clean || true

execute: |
  arch="$(dpkg --print-architecture)"

  run_sdkcraft pack
  test -f "my-sdk-name_${arch}_ubuntu@22.04.sdk"
