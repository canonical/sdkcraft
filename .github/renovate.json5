{
  extends: ["github>canonical/starflow:renovate.json5"],
  packageRules: [
    {
      matchPackageNames: ["craft-application"],
      allowedVersions: "<7",
    },
  ],
  // Override starflow because we don't run any checks on renovate/* branches.
  prCreation: "immediate",
}
